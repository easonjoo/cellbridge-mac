# CellBridge 发布说明（2026-09-12）

本地 Mac + 4G 模块（Quectel EG25-G / 拜为 QDC507）当 SIP 网关，iPhone 经 Linphone 接入，
实现局域网/外网打电话、CallKit 推送唤醒、收发短信。本-release 重点修「锁屏下来电不推送」，
并补了模块温度可观测性 + 凭据安全。

## 本次改动

### 1. 锁屏/休眠来电推送修复（头号 bug）
- `internal/sip/linphone_push.go`：FlexiAPI 推送改为支持「按账号推送」。手机默认**不**把
  VoIP/Push token 报到 cellbridge（Contact 里没有 `pn-param`/`pn-prid`），原逻辑因此永远不推送。
  现在 `wakeLinphoneClients` 在没有客户端上报 token 时，退化为用 `linphone_push_from` 账号向
  sip.linphone.org 发起 account-based 推送（`pn_param`/`pn_prid` 传 JSON null），锁屏/休眠也能唤醒。
- `sendLinphonePush`：守卫从「三参数全非空」放宽为「只要 `pn_provider` 非空」；payload 改用
  `map[string]any`，空 token 字段传 `null`（FlexiAPI 按账号推送的合法用法）。
- `storePushParams`：手机以 `expires=0` unregister（锁屏会注销）时**不再清除**已存推送参数，
  避免锁屏后来电无 token 可推。

> 仍需你早上确认的两点（非代码问题）：
> - **FlexiAPI Key 已失效**：日志 `key_prefix=vjehKx… key_len=40`，但用真/假 Key 各推一次均返回
>   `Invalid API Key`。需在 sip.linphone.org 重新生成 x-api-key，写入 `~/.cellbridge/linphone_push_key`
>   （脚本会注入 `CB_LINPHONE_PUSH_KEY`），并重启网关。
> - **iPhone 需在线到 Tailscale**：测试时 `tailscale status` 里 iphone184 显示 offline 则外网推送/通话到不了。

### 2. 模块温度可观测性 + 降热
- `internal/modem/at/adapter.go`：新增 `Temperature()`，读 `AT+QTEMP` / `AT+CPUTEMP?`，解析首个数值。
- `cmd/cellbridge-gateway/main.go`：新增 `thermalMonitor`，每 30s 读一次模块温度并打日志；
  `≥60°C` 告警（实测本机长期 64–67°C）。
- SMS 收件箱轮询默认 `5s → 15s`（`start_cellbridge.sh` 的 `CELLBRIDGE_SMS_POLL_INTERVAL`，可调），
  降低模块空转热。ghostReaper（10s 扫 `AT+CLCC`）保留——它只在无活动会话时跑，且防幽灵呼叫，不动。

### 3. 凭据安全（public 仓库前必做）
- `start_cellbridge.sh` 不再内置弱口令默认值。SIP 口令改从 gitignored 的 `cellbridge.secrets.env`
  读取（模板见 `cellbridge.secrets.env.example`）；缺失则报错退出，绝不提交明文口令。
- `cellbridge.secrets.env` 已加入 `.gitignore`；当前真实值已写入本地该文件，**运行不受影响**。
- README 口令示例改为「由 secrets.env 注入」，不再暴露 `cellbridge-idoer`。
- 测试 fixture 里的真实号码 `15688525123` 替换为测试号 `13800138000`。

## 推送架构（单一可信路径）
入站蜂窝来电 → SIP `inboundLoop` → `ringClients` → `wakeLinphoneClients`（FlexiAPI，见上）。
`api/server.go` 的 broker / `sip/yakpush.go` 为备用通道，未配置时为空转（日志 `push skipped
sender unavailable` / `yakpush skipped`），不影响主路径。想用 APNs broker 或 YakPush 需另行配置
`Push.Mode=broker` 与 `YakPushToken`。

## 部署
```bash
./rebuild-gateway.sh                 # 覆盖 gateway-patched 并编译出 cellbridge-gateway.new
./start_cellbridge.sh stop
mv cellbridge-gateway.new cellbridge-gateway
./start_cellbridge.sh                # 或 python3 /tmp/daemonize_start.py 脱钩启动
```
健康：`curl http://127.0.0.1:8787/api/v1/health` 应返回 `{"status":"ok"}`。
