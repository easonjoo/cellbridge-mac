# CellBridge SIP 凭据模板（提交到仓库；不含真实口令）
# 用法：
#   cp cellbridge.secrets.env.example cellbridge.secrets.env
#   把下面四个值改成你自己的强口令，并存为 cellbridge.secrets.env（已被 .gitignore 忽略）
# start_cellbridge.sh 启动时 source cellbridge.secrets.env；若该文件不存在则从环境变量读取；
# 两者都没有则直接报错退出（不再内置弱口令默认值）。
SIP_USER=iphone
SIP_PASS=change-me-to-a-strong-random-password
SIP_USER2=sheldon
SIP_PASS2=change-me-to-a-strong-random-password
